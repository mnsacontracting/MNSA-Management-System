import streamlit as st
import pandas as pd

st.set_page_config(page_title="نظام MNSA لإدارة المناقصات", layout="wide")

st.title("🏗️ نظام إدارة المناقصات والعقود الذكي")
st.sidebar.header("القائمة الرئيسية")

# خيارات النظام
menu = ["تحليل مناقصة جديدة", "سجل العقود والمناقصات", "المستخلصات والماليات"]
choice = st.sidebar.selectbox("اختر المهمة", menu)

if choice == "تحليل مناقصة جديدة":
    st.subheader("📁 رفع وتحليل ملفات المناقصة")
    uploaded_file = st.file_uploader("ارفع ملف المناقصة (PDF, Image, Excel)", type=['pdf', 'png', 'jpg', 'xlsx'])
    
    if uploaded_file:
        st.success("تم استلام الملف بنجاح! جاري التجهيز لربط الذكاء الاصطناعي...")
        if st.button("بدء التحليل واستخراج البيانات"):
            st.info("هنا سيظهر تحليل البنود (خرسانة، طرق، إلخ) ويرحل للشيت.")

elif choice == "سجل العقود والمناقصات":
    st.subheader("📊 قاعدة البيانات الحالية")
    st.write("سيتم عرض بيانات جوجل شيت هنا قريباً.")

else:
    st.subheader("💰 إدارة المستخلصات")
    st.write("قسم حسابات الموردين والجهات المنفذة.")
